---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';

const allEvents = [
    {
        date: '3月17日(金)',
        events: [
            { start: '14:00', end: '14:45', name: '深澤知', avatar: 'fukasawa.jpg', title: '例をつくる、地に足をつける、ノートを見ない'},
            { start: '15:00', end: '15:45', name: '原本博史', avatar: 'haramoto.jpg', title: 'いくつかの擬似乱数に関する統計的・非統計的な評価について'},
            { start: '16:00', end: '16:45', name: '渡辺業', title: 'コラッツ予想の2進整数への拡張とその応用'},
        ],
    },
    {
        date: '3月18日(土)',
        events: [
            { start: '10:00', end: '10:45', name: '東根一樹', title: 'Galois lines for the maximal curve of the second largest genus in characteristic two'},
            { start: '11:00', end: '11:45', name: '田端亮', title: '量子力学に現れる Schur power matrix'},
            { start: '14:00', end: '14:45', name: '木村俊一', avatar: 'kimura.jpg', title: 'もしもコラッツ予想を真剣に考えている時に、誰か他の人が解決してしまったら、明日からは一体何を考えれば良いのだろうか？'},
            { start: '15:00', end: '15:45', name: '奥田俊一朗', title: '高校数学における思考力・判断力・表現力の向上を働きかける実践'},
        ],
    },
]
---

<Layout title="代数学ワークショップ">
    <header>
		<h1><img src="logo.svg" alt="代数学ワークショップ" class="logo"></h1>
        
        <p>2023年3月17日(金)—18日(土), 
        広島大学</p>
    </header>
	<main>
        <h2>会場</h2>
        <p>広島大学理学部 B707 (<a href="https://www.hiroshima-u.ac.jp/access/higashihiroshima">広島大学 東広島キャンパスへのアクセス</a>)</p>

        <aside class="instructions">
            <span class="instruction-icon">🧡</span>
            <div class="instruction-message">
                コロナウィルス感染予防のため、参加者の把握にご協力ください。
                参加を予定される方は必ず深澤または世話人のいずれかへご連絡ください。
            </div>
        </aside>
        
        
        <h2>プログラム</h2>
        { allEvents.map((day) =>
        <h3 class="date">{day.date}</h3>
        <>
            {day.events.map((event) =>
            <div class="schedule-item">
                <div class='schedule-time'>
                    <span class='schedule-time-begin'>{event.start}</span>
                    <span class='schedule-time-end'>{event.end}</span>
                </div>
                <div class='bar'/>
                <div class='talk'>
                    <figure class='person-picture-s' >
                        <img src={event.avatar ? `images/${event.avatar}` : 'images/general-person-silhouette.png'} />
                    </figure>
                    <span class='speaker-name'>{event.name}</span>
                    <div>
                        <span class='title'>{event.title}</span>
                    </div>
                    <div class='abstract'>{event.abstract}</div>
                </div>
            </div>
            )}
        </>
        )}

        <div class="instructions">
            <span class="instruction-icon">🎂</span>
            <div class="instruction-message">
                ワークショップ終了後、ささやかですが、木村俊一先生の還暦をお祝いする会を催します。
            </div>
        </div>


        <h2>Organizers</h2>
        <dl>
            <dt>世話人</dt><dd>奥田俊一朗、深澤知、原本博史、田端亮、沖吉真実</dd>
            <dt>現地世話人</dt><dd>高橋宣能、北䑓如法</dd>
        </dl>

	</main>
    <footer>
        代数学ワークショップ
    </footer>
</Layout>

<style>
 header {
     border: 9px solid rgb(var(--main));
     margin: 0;
     padding: 0;
     text-align: center;
     color: rgb(var(--main));
     font-weight: bold;
 }
 .logo {
     max-width: 680px;
 }
 p {
     line-height: 1.6;
 }
 main {
	 margin: auto;
	 padding: 1.1rem;
	 max-width: 60ch;
 }
 h1 {
	 font-size: 3rem;
	 font-weight: 800;
	 margin: 0;
     border-bottom: 1px solid rgb(var(--main));
 }
 h1 img {
     margin: 16px 28px;
 }
 header p {
     margin: 16px 0;
 }
 h2 {
     color: rgb(var(--main));
     font-size: 1.87em;
 }
 h3 {
     color: #666;
 }
 h3.date {
     margin: 2.5em 0 0 0;
 }
 .text-gradient {
	 background-image: var(--accent-gradient);
	 -webkit-background-clip: text;
	 -webkit-text-fill-color: transparent;
	 background-size: 400%;
	 background-position: 0%;
 }
 .instructions {
	 line-height: 1.6;
	 margin: 1rem 0;
	 border: 2px solid rgba(var(--main), 25%);
	 background-color: white;
	 padding: 1rem;
	 border-radius: 0.4rem;

     display: flex;
     align-items: flex-start;
 }
 .instruction-icon {
     align-self: center;
 }
 .instruction-message {
     flex: 1;
     padding-left: .5em;
 }
 .instructions code {
	 font-size: 0.875em;
	 font-weight: bold;
	 background: rgba(var(--accent), 12%);
	 color: rgb(var(--accent));
	 border-radius: 4px;
	 padding: 0.3em 0.45em;
 }
 .instructions strong {
	 color: rgb(var(--accent));
 }
 .link-card-grid {
	 display: grid;
	 grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
	 gap: 1rem;
	 padding: 0;
 }
 footer {
     background-color: rgb(var(--main));
     margin: 0;
     padding: 1em;
     text-align: center;
     color: white;
 }
 .schedule-item {
     margin-top: 2em;
     text-align: left;
     display: flex;
     flex-direction: row;
 }

 .schedule-time {
     font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", "Segoe UI", sans-serif;
     font-weight: normal;
     display: flex;
     justify-content: space-between;
     flex-direction: column;
 }

 .schedule-time-begin {
     font-size: smaller;
 }

 .schedule-time-end {
     font-size: smaller;
     color: #aaa;
 }

 .bar {
     background-color: rgb(var(--main));
     flex: 0 0 6px;
     margin: 0 18px;
     border-radius: 3px
 }

 .talk .title {
     display: inline;
     font-family: "Hiragino Maru Gothic W4 JIS2004", YuGothic, "Hiragino Kaku Gothic ProN", "Yu Gothic", 游ゴシック体, "Hiragino Kaku Gothic Pro", Meiryo, "Helvetica Neue", Helvetica, Arial, "Avenir Next", Quicksand, sans-serif;
     font-size: 1.0em;
 }

 .talk .speaker-name {
     font-size: larger;
     font-weight: bold;
     vertical-align: middle;
 }

 .talk figure {
     display: inline-block;
     vertical-align: middle;
     margin: 8px 8px 8px 0;
 }

 .talk .abstract {
     font-size: small;
     line-height: 1.6;
 }

 main {
     --avatar-size-s: 32px;
     --avatar-size: 48px;
 }
 firgure {
     margin: 0;
     padding: 0;
 }
 .person-picture-s img {
     margin: 0;
     padding: 0;
     width: var(var(--avatar-size-s));
     height: var(--avatar-size-s);
     border-radius: var(--avatar-size-s);
     background-size: var(--avatar-size-s);
     object-fit: cover;
 }
 @media (min-width: 600px) {
     .person-picture-s img {
         width: var(--avatar-size);
         height: var(--avatar-size);
         border-radius: var(--avatar-size);
         background-size: var(--avatar-size);
     }
 }
</style>
